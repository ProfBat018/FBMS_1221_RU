#include <iostream>

using namespace std;

int main()
{
	int number = 6;
	// number % 2 == 0 ? 50 : 20
	//number < 0 ? 10 : number == 5 ? 20 : 60

	//bool result = (number == 6 ? true : false);
	
	//cout << result << endl;

	return 0;
}
